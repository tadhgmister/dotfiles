;; alias to tmp-os defined in initial setup module.  eventually I will
;; get my stuff together enough to actually reference the module from
;; all initial setup scripts and remove this file

(use-modules (tadhg initial-setup))
tmp-os
